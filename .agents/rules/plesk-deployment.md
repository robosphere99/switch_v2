# Plesk IIS Deployment Rules

1. NEVER create or modify `web.config` anywhere in the repository.
2. IIS `web.config` is auto-managed by Plesk natively on the server.
3. Keep `web.config` in `.gitignore` permanently.
